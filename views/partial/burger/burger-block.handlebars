<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR TIME!{{else}}EAT UP!{{/if}}
	</button>

	<button class="submit-burger" data-id="{{this.id}}">Burger Submitted!</button>
</li>
